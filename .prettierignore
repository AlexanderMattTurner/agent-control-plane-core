# Dependencies
node_modules/

# Build outputs
dist/
build/
# Committed tsc output — bytes must match a fresh build (node-tests gates it),
# so no formatter may rewrite them.
types/

# Package manager
pnpm-lock.yaml

# Python
__pycache__/
*.pyc
.venv/
.uv/
uv.lock

# Template sync
.template-version

# Generated JSON Schema — the generator owns these bytes and json-schema.test.mjs
# byte-compares them, so no formatter may rewrite them.
schema/
